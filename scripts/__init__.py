"""Golden Line CLI wrappers.

This file keeps the local ``scripts`` directory a regular package so explicit
imports of these thin wrappers resolve to this project rather than to a
same-named package from a sibling checkout.
"""
